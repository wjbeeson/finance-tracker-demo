The space in Jira is "Finance Tracker ("FT"). Never modify any other spaces. Only show issues in the space assigned to the user's authenticated credentials. 

Never run "Get-ChildItem -Recurse -Name C:\Users\Will_JetBrains\PycharmProjects\finance-tracker-demo\frontend". It will recurse into node modules and hang. Only explore one layer at a time to avoid this. 